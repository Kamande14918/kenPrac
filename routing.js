// Using the # symbol for routing
// The part after the # doesn't trigger a full page reload.
// We can use it to manage navigation in a single-page application


// window.addEventListener("hashchange", () => {
//   const route = location.hash.slice(1); // e.g., "#/about" → "about"
//   renderView(route);
// });

// function renderView(route) {
//   const container = document.getElementById("app");
//   if (route === "home") container.innerHTML = "<h1>Home Page</h1>";
//   else if (route === "about") container.innerHTML = "<h1>About Page</h1>";
//   else container.innerHTML = "<h1>404 Not Found</h1>";
// }


// 2>